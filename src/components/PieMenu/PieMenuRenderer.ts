// Placeholder: PieMenu canvas renderer
// Responsible for drawing pie slices on a canvas element

export interface RenderOptions {
  centerX: number;
  centerY: number;
  radius: number;
  sliceCount: number;
}

export function renderPieMenu(
  _ctx: CanvasRenderingContext2D,
  _options: RenderOptions,
): void {
  // TODO: implement canvas rendering
}
